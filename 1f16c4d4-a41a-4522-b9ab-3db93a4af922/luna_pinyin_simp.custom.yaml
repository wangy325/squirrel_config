# User luna_pinyin_simp config file.
# encoding utf-8

# work with luna_pinyin.custom.yaml

__patch:
  - patch/+:
      __include: emoji_suggestion:/patch # emoji需要重新配置

patch:
  # 模糊音
  speller/algebra:
    - erase/^xx$/                      # 第一行保留
    #- derive/^([zcs])h/$1/             # zh, ch, sh => z, c, s
    #- derive/^([zcs])([^h])/$1h$2/     # z, c, s => zh, ch, sh
    - derive/([aei])n$/$1ng/            # an => ang en => eng, in => ing
    - derive/([aei])ng$/$1n/            # ang => an eng => en, ing => in 
    - derive/([u])an$/$1ang/            # uan => uang
    - derive/([u])ang$/$1an/            # uang => uan